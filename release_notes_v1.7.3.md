# ForkNews v1.7.3 - Улучшения диагностики

## 🗑️ Удалено

### Секция GitHub Token из настроек
- Удалено поле ввода GitHub Token из настроек
- Удалена кнопка помощи "Как получить токен?"
- Токен всё ещё используется (если был установлен ранее), но UI управления удалён
- Это упрощает интерфейс приложения

## ✨ Новые возможности

### 📤 Кнопка "Поделиться" в диагностике
- В диалоге диагностики (кнопка "?" в меню) добавлена кнопка **"Поделиться"**
- Создаёт файл с именем `log_YYYY-MM-DD_HH-mm-ss.txt`
- Отправляет лог через стороннее приложение (email, мессенджер, облако и т.д.)
- Формат имени файла: `log_2026-01-10_15-30-45.txt`

### 🔄 Обновлённый диалог диагностики
- **Кнопки**:
  - "Скопировать" - копирует лог в буфер обмена
  - "Поделиться" - отправляет лог файлом через Intent
  - "Очистить" - очищает все логи

## 🛠️ Технические детали

### Добавленные компоненты:
- `FileProvider` в AndroidManifest.xml для безопасного обмена файлами
- `file_paths.xml` с конфигурацией cache-path
- Метод `shareLogFile()` в MainActivity для создания и отправки файла

### Изменённые файлы:
- `MainActivity.kt`: Добавлен метод `shareLogFile()`, обновлён `showDiagnosticDialog()`
- `SettingsActivity.kt`: Удалены методы `setupGitHubToken()`, `showTokenHelpDialog()`, убрана загрузка токена
- `activity_settings.xml`: Удалена секция MaterialCardView с GitHub Token
- `AndroidManifest.xml`: Добавлен FileProvider
- `file_paths.xml`: Новый файл с конфигурацией путей

### Реализация shareLogFile():
```kotlin
private fun shareLogFile(logs: String) {
    val timestamp = SimpleDateFormat("yyyy-MM-dd_HH-mm-ss", Locale.getDefault()).format(Date())
    val fileName = "log_$timestamp.txt"
    val file = File(cacheDir, fileName)
    file.writeText(logs)
    
    val uri = FileProvider.getUriForFile(
        this,
        "${applicationContext.packageName}.fileprovider",
        file
    )
    
    val shareIntent = Intent(Intent.ACTION_SEND).apply {
        type = "text/plain"
        putExtra(Intent.EXTRA_STREAM, uri)
        putExtra(Intent.EXTRA_SUBJECT, "ForkNews Diagnostic Log")
        addFlags(Intent.FLAG_GRANT_READ_URI_PERMISSION)
    }
    
    startActivity(Intent.createChooser(shareIntent, "Поделиться логом"))
}
```

## 💡 Использование

### Как поделиться логом:
1. Откройте меню (три точки)
2. Нажмите кнопку **"?"** (Диагностика)
3. Нажмите **"Поделиться"**
4. Выберите приложение для отправки (Email, Telegram, WhatsApp, Drive и т.д.)
5. Файл будет отправлен с именем `log_дата_время.txt`

## 📱 Информация о сборке
- **Версия**: 1.7.3
- **Version Code**: 31
- **Min SDK**: 33 (Android 13)
- **Target SDK**: 34
- **SHA256**: `8748e0e879d4a09818788c52799f5c9d8d7ec40c5777933a45a33df85d2d6edc`

## 🔄 Обратная совместимость
- GitHub Token, установленный в v1.7.1, продолжит работать
- Токен хранится в DataStore и используется RetrofitClient
- Просто удалён UI для управления токеном

---

**Полный changelog**: v1.7.2...v1.7.3
