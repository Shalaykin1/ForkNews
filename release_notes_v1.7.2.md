# ForkNews v1.7.2 - Изменение частоты проверки

## 🔄 Изменения в частоте проверки обновлений

### Увеличен интервал проверки по умолчанию
- **Было**: Проверка каждую 1 минуту (в настройках и при открытом приложении)
- **Стало**: Проверка каждые 5 минут (в настройках и при открытом приложении)

## 📊 Подробности изменений

### В настройках приложения:
- Первый пункт в "Частота проверки" изменен с **"1 минута"** на **"5 минут"**
- Значение по умолчанию изменено с 1 минуты на 5 минут
- Доступны варианты: 5 минут, 30 минут, 1 час, 2 часа, 6 часов, 12 часов, Пользовательское время

### При открытом приложении:
- Автообновление в фоновом режиме теперь происходит каждые 5 минут (было 60 секунд)
- Это снижает нагрузку на GitHub API и экономит трафик

## 💡 Зачем это изменение?

С учетом того, что в v1.7.1 добавлена поддержка GitHub Personal Access Token (лимит 5000 запросов/час), проверка каждую минуту больше не требуется. Интервал в 5 минут:
- ✅ Более разумное использование API (даже с токеном)
- ✅ Снижение энергопотребления устройства
- ✅ Уменьшение трафика данных
- ✅ Релизы всё равно проверяются достаточно часто

## 🔐 Напоминание о GitHub Token

Если вы ещё не добавили GitHub Personal Access Token, рекомендуем это сделать:
1. Откройте: https://github.com/settings/tokens
2. Generate new token (classic) → без permissions
3. Вставьте в Настройки → GitHub API Token

Токен увеличивает лимит API с 60 до 5000 запросов/час и предотвращает ошибки "API rate limit exceeded".

## 📱 Информация о сборке
- **Версия**: 1.7.2
- **Version Code**: 30
- **Min SDK**: 33 (Android 13)
- **Target SDK**: 34
- **SHA256**: `5bc7970193cf6bf8507bf542488739bc881d70f8cca0afabd4c20d624ae4f9b6`

## 🛠️ Технические детали

### Измененные файлы:
- `PreferencesManager.kt`: Дефолтное значение `CHECK_INTERVAL_KEY` изменено с 1L на 5L
- `SettingsActivity.kt`: Первый пункт списка изменен на "5 минут", маппинг интервалов обновлен
- `MainActivity.kt`: Интервал `Handler.postDelayed()` изменен с 60_000 (1 минута) на 300_000 (5 минут)

### Изменения в коде:
```kotlin
// PreferencesManager.kt
preferences[CHECK_INTERVAL_KEY] ?: 5L // Default 5 minutes (было 1L)

// SettingsActivity.kt
val intervals = arrayOf("5 минут", ...) // было "1 минута"
val minutes = when (position) {
    0 -> 5L  // было 1L
    ...
}

// MainActivity.kt
handler.postDelayed(this, 300_000) // 5 минут (было 60_000)
```

---

**Полный changelog**: v1.7.1...v1.7.2
