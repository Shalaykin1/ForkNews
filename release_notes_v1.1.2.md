# ForkNews v1.1.2

## 🎯 Основные улучшения

### ✨ Автоматическая проверка при добавлении
- ✅ **GitHub**: При добавлении репозитория сразу проверяется и отображается последний релиз
- ✅ **GameHub**: При добавлении сразу загружается и показывается имя доступного APK
- ✅ Больше не нужно ждать первой фоновой проверки - информация появляется мгновенно

### 🔔 Улучшена система уведомлений
- ✅ Добавлено логирование для отладки уведомлений
- ✅ Улучшена обработка ошибок при показе уведомлений
- ✅ Более надежное создание notification channel

### 📋 Показ информации о релизах
- ✅ Вместо "Нет релизов" для GitHub сразу отображается актуальная версия
- ✅ Вместо "Нет релизов" для GameHub показывается название APK файла
- ✅ Информация обновляется сразу после добавления репозитория

## 🔧 Технические изменения

### Новые методы
- `RepositoryRepository.getRepositoryById()` - получение репозитория по ID
- Автоматический вызов `checkForUpdates()` после `addRepository()`

### Изменения в MainViewModel
```kotlin
fun addRepository(url: String) {
    val repoId = repository.addRepository(repo)
    val addedRepo = repository.getRepositoryById(repoId)
    if (addedRepo != null) {
        repository.checkForUpdates(addedRepo)  // Сразу проверяем релиз
    }
}

fun addGameHub() {
    val repoId = repository.addRepository(gameHub)
    val addedRepo = repository.getRepositoryById(repoId)
    if (addedRepo != null) {
        repository.checkForUpdates(addedRepo)  // Сразу проверяем APK
    }
}
```

### Улучшения UpdateCheckWorker
- Добавлен try-catch при показе уведомлений
- Добавлено логирование успешных и неуспешных уведомлений
- Улучшена отладка проблем с уведомлениями

## 📱 Информация о сборке

- **Версия**: 1.1.2 (versionCode: 7)
- **Размер APK**: 14 MB
- **SHA-256**: `35ebee5566bcc4e12139de83ba95982fc9f86451cc907ee29c55651aff8ae618`
- **Минимальная версия Android**: 13 (API 33)

## 📥 Установка

### Новые пользователи
```bash
wget https://github.com/Shalaykin1/ForkNews/releases/download/v1.1.2/ForkNews-v1.1.2.apk
adb install ForkNews-v1.1.2.apk
```

### Обновление с v1.1.1
```bash
adb install -r ForkNews-v1.1.2.apk
```

## ✅ Что изменилось с v1.1.1

| Функция | v1.1.1 | v1.1.2 |
|---------|--------|--------|
| Показ релиза при добавлении GitHub | ❌ (ждет фоновой проверки) | ✅ (мгновенно) |
| Показ APK при добавлении GameHub | ❌ (ждет фоновой проверки) | ✅ (мгновенно) |
| Логирование уведомлений | ❌ | ✅ |
| Обработка ошибок уведомлений | Базовая | Улучшенная |

## 🔍 Как это работает

### При добавлении GitHub репозитория
1. Пользователь вводит URL: `https://github.com/owner/repo`
2. Репозиторий добавляется в базу данных (возвращается ID)
3. **Сразу же** выполняется проверка через GitHub API
4. Последний релиз (например, `v1.5.0`) сохраняется в БД
5. UI обновляется и показывает `v1.5.0` вместо "Нет релизов"

### При добавлении GameHub
1. Пользователь выбирает "GameHub"
2. Репозиторий добавляется в базу данных
3. **Сразу же** парсится страница https://gamehub.xiaoji.com/download/
4. Название APK (например, `gamehub_v2.5.apk`) сохраняется в БД
5. UI показывает `gamehub_v2.5.apk` вместо "Нет релизов"

### Уведомления
- При каждом показе уведомления в лог пишется сообщение
- Если произошла ошибка, она логируется с деталями
- Помогает диагностировать проблемы с уведомлениями

## 🐛 Исправленные проблемы

- ✅ При добавлении репозитория показывалось "Нет релизов" до первой фоновой проверки
- ✅ Для GameHub не отображалась информация об APK файле
- ✅ Сложно было диагностировать проблемы с уведомлениями

## 💡 Примеры использования

### Добавление репозитория с мгновенным показом релиза
```
1. Нажать "+" → "GitHub репозиторий"
2. Ввести: https://github.com/microsoft/vscode
3. Нажать "Добавить"
4. ✨ Сразу видим "1.85.0" вместо "Нет релизов"
```

### Проверка уведомлений
```
adb logcat | grep UpdateCheckWorker

Вы увидите:
D/UpdateCheckWorker: Notification shown: VSCode - v1.85.0
```

## 🔒 Разрешения

Без изменений:
- `INTERNET` - для сетевых запросов
- `POST_NOTIFICATIONS` - для уведомлений
- `SCHEDULE_EXACT_ALARM` - для точных проверок
- `RECEIVE_BOOT_COMPLETED` - для автозапуска

## 📚 Документация

- [README.md](README.md) - Обзор проекта
- [ARCHITECTURE.md](ARCHITECTURE.md) - Архитектура
- [CHANGELOG.md](CHANGELOG.md) - История изменений
- [FAQ.md](FAQ.md) - Часто задаваемые вопросы

## 🔗 Полезные ссылки

- **Репозиторий**: https://github.com/Shalaykin1/ForkNews
- **Issues**: https://github.com/Shalaykin1/ForkNews/issues
- **Все релизы**: https://github.com/Shalaykin1/ForkNews/releases

## 💬 Обратная связь

Нашли баг или есть предложение? Создайте Issue на GitHub!

## ⚠️ Важно

Приложение подписано тем же ключом, что и v1.1.1. Обновление произойдет без потери данных.

---

**Спасибо за использование ForkNews! 🎉**
