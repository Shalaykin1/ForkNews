# Финальные шаги для создания релиза

Код успешно загружен в GitHub! Теперь вам нужно выполнить сборку APK локально.

## Шаг 1: Клонируйте проект на локальную машину

```bash
git clone https://github.com/Shalaykin1/ForkNews.git
cd ForkNews
```

## Шаг 2: Установите необходимые инструменты

### Установите Android Studio
1. Скачайте с https://developer.android.com/studio
2. Установите Android SDK 34 через SDK Manager
3. Установите JDK 17 (если еще не установлен)

### Или используйте командную строку
Если у вас уже есть Android SDK:

```bash
# Убедитесь, что установлен JDK 17
java -version  # Должен показать Java 17

# Установите ANDROID_HOME
export ANDROID_HOME=/path/to/your/android-sdk
export PATH=$PATH:$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools
```

## Шаг 3: Соберите APK

### Вариант А: Используя скрипт автоматизации (рекомендуется)

```bash
# Сделайте скрипт исполняемым (если еще не сделано)
chmod +x release.sh

# Запустите сборку
./release.sh 1.0.0
```

Скрипт автоматически:
- Создаст keystore (если его нет)
- Соберет release APK
- Подпишет его
- Создаст SHA-256 контрольную сумму
- Создаст git тег
- Создаст релиз на GitHub с подписанным APK

### Вариант Б: Android Studio

1. Откройте проект в Android Studio
2. Выберите `Build -> Generate Signed Bundle / APK`
3. Выберите `APK`
4. Создайте новый keystore:
   - Key store path: `forknews.keystore`
   - Password: придумайте и сохраните
   - Key alias: `forknews`
   - Key password: придумайте и сохраните
   - Validity: 25 лет
5. Выберите `release` build variant
6. Нажмите `Finish`

APK будет в `app/build/outputs/apk/release/app-release.apk`

### Вариант В: Gradle командная строка

```bash
# 1. Создайте keystore
keytool -genkey -v -keystore forknews.keystore \
  -alias forknews \
  -keyalg RSA \
  -keysize 2048 \
  -validity 10000

# 2. Добавьте настройки подписи в app/build.gradle.kts
# (См. детали в RELEASE_BUILD.md)

# 3. Соберите подписанный APK
./gradlew clean assembleRelease

# APK будет в app/build/outputs/apk/release/app-release.apk
```

## Шаг 4: Создайте релиз на GitHub

### Автоматически (если использовали release.sh)
Скрипт уже создал релиз!

### Вручную через веб-интерфейс

1. Перейдите на https://github.com/Shalaykin1/ForkNews/releases
2. Нажмите **"Draft a new release"**
3. Заполните форму:
   - **Tag version**: `v1.0.0`
   - **Release title**: `ForkNews v1.0.0 - Initial Release`
   - **Description**:
   ```markdown
   ## ForkNews v1.0.0 - Первый релиз! 🎉

   ### Возможности
   - ✅ Мониторинг обновлений GitHub репозиториев
   - ✅ Поддержка GameHub (APK файлы)
   - ✅ Настраиваемые интервалы проверки (30 мин - 12 часов)
   - ✅ Фоновые проверки с уведомлениями
   - ✅ Светлая/тёмная тема
   - ✅ Индивидуальные настройки уведомлений для каждого репозитория
   - ✅ Свайп для удаления

   ### Системные требования
   - Android 13 (API 33) или выше
   - Минимум 50 МБ свободного места

   ### Установка
   1. Скачайте `app-release.apk`
   2. Разрешите установку из неизвестных источников
   3. Установите APK
   4. Откройте приложение и добавьте репозитории

   ### SHA-256
   См. файл `app-release.apk.sha256`

   ### Документация
   - [README](README.md)
   - [Быстрый старт](QUICKSTART.md)
   - [Архитектура](ARCHITECTURE.md)
   - [FAQ](FAQ.md)
   ```

4. Прикрепите файлы:
   - `app-release.apk` (подписанный APK)
   - `app-release.apk.sha256` (контрольная сумма)

5. Нажмите **"Publish release"**

### Через GitHub CLI (gh)

```bash
# Создайте релиз
gh release create v1.0.0 \
  app/build/outputs/apk/release/app-release.apk \
  --title "ForkNews v1.0.0 - Initial Release" \
  --notes "Первый релиз приложения для мониторинга GitHub репозиториев"

# Добавьте SHA-256
sha256sum app/build/outputs/apk/release/app-release.apk > app-release.apk.sha256
gh release upload v1.0.0 app-release.apk.sha256
```

## Готово! 🎉

После выполнения этих шагов у вас будет:
- ✅ Подписанный APK файл
- ✅ Релиз на GitHub с тегом v1.0.0
- ✅ Доступное для скачивания приложение

## Дополнительная информация

- **Подробная инструкция по сборке**: [RELEASE_BUILD.md](RELEASE_BUILD.md)
- **Автоматизация CI/CD**: [.github/workflows/android-ci.yml](.github/workflows/android-ci.yml)
- **Безопасность**: Сохраните keystore и пароли в безопасном месте! Они понадобятся для будущих обновлений.

## Устранение проблем

### "SDK location not found"
```bash
# Создайте local.properties
echo "sdk.dir=/path/to/your/android-sdk" > local.properties
```

### "Java version incompatible"
```bash
# Установите JDK 17
sdk install java 17.0.9-ms
sdk use java 17.0.9-ms
```

### "Keystore not found"
```bash
# Создайте новый keystore
keytool -genkey -v -keystore forknews.keystore -alias forknews \
  -keyalg RSA -keysize 2048 -validity 10000
```

---

**Нужна помощь?** Откройте issue на GitHub: https://github.com/Shalaykin1/ForkNews/issues
