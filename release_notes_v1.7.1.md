# ForkNews v1.7.1 - Исправление лимита GitHub API

## 🔧 Критическое исправление

### Решена проблема "API rate limit exceeded"
- **Проблема**: GitHub API ограничивает неавторизованные запросы до 60 в час с одного IP-адреса
- **Решение**: Добавлена поддержка GitHub Personal Access Token
- **Результат**: Лимит увеличивается с 60 до 5000 запросов в час

## ✨ Новые возможности

### 🔐 Поддержка GitHub Personal Access Token
- Новая секция в настройках: "GitHub API Token"
- Поле ввода токена с возможностью скрыть/показать
- Кнопка помощи "?" с подробными инструкциями по созданию токена
- Автоматическое сохранение токена при потере фокуса

### 📊 Улучшенная диагностика
- Логирование статуса токена (установлен/не установлен)
- Отображение первых 4 символов токена в логах (для безопасности)
- Подробные сообщения об ошибках HTTP 403 с объяснением rate limit

## 🔐 Как получить GitHub Token

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Введите имя токена (например, "ForkNews App")
4. **Не выбирайте никаких разрешений** (permissions) - для публичных репозиториев они не нужны
5. Нажмите "Generate token"
6. Скопируйте токен (показывается только один раз!)
7. Вставьте токен в настройки ForkNews → "GitHub API Token"

## 🔒 Безопасность
- Токен хранится локально на устройстве в зашифрованном DataStore
- Токен добавляется только в заголовок Authorization для запросов к api.github.com
- В логах отображаются только первые 4 символа токена

## 📋 Технические детали

### Изменения в коде:
- `PreferencesManager.kt`: Добавлены методы `setGitHubToken()`, `getGitHubToken()`, `getGitHubTokenSync()`
- `RetrofitClient.kt`: Добавлен `authInterceptor` для инъекции токена в заголовок Authorization
- `SettingsActivity.kt`: Новая UI секция для управления токеном + диалог помощи
- `RepositoryRepository.kt`: Расширенное логирование статуса токена и HTTP ошибок

### API лимиты:
- **Без токена**: 60 запросов/час (быстро исчерпывается при 5 репозиториях)
- **С токеном**: 5000 запросов/час (достаточно для любого использования)

## 📱 Информация о сборке
- **Версия**: 1.7.1
- **Version Code**: 29
- **Min SDK**: 33 (Android 13)
- **Target SDK**: 34
- **SHA256**: `b1ac7d0cd47fea231faab7b6513f054c3993900cf0401179569f211502addfd0`

## 🐛 Исправленные проблемы
- Исправлена ошибка "HTTP 403: API rate limit exceeded for [IP]"
- Все репозитории теперь корректно загружают информацию о релизах
- Устранено сообщение "Нет релизов" для существующих репозиториев

## 💡 Рекомендации
**Настоятельно рекомендуется добавить GitHub Token после установки этого обновления**, иначе при частой проверке обновлений вы можете столкнуться с лимитом API.

---

**Полный changelog**: v1.7.0...v1.7.1
